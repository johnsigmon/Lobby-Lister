<!DOCTYPE html>
<html>
  <head><% include ../partials/head %></head>
    <body >
    <div class="header">
      <nav><% include ../partials/navigation %></nav>
        <div class="container">
        <h3 class="center">Top 10 Payments in the past year.</h3>
        <section id="chart">

        </section>
        <div class="row">
        <div class="col s10 offset-s1">
          <%if(companies.length){ %>
          <% companies.forEach( function(companies){  %>
        <div class="card grey darken-1">
        <div class="card-content white-text">
        <div class="center">
        <span class="card-title"><%=companies.client_name%></span>
        </div>
          <div class="center">
            <span class="money">
            $<strong><%=companies.amount%></strong>
            </span>
            </div>
          <p>
          <span class="registrar">
          Registrant:
          </span>
          <%=companies.registrant_name%>
            <%if(companies.registrant_general_description){ %>
           (Description:  <%=companies.registrant_general_description%>)
           <% } %>
          </p>
          <p>
          <span class="lobbyist">
          <%=companies.contact_full_name%>
            </span>
          was most likely, the lobbyist retained for this particular issue. </p>
          <p><span class="transactionID">Transaction ID: </span><%=companies.id%></p>
          <p class="center">(use for further research)</p>
          </div>
          <div class="card-action center">
            <form action='search/details' method='get' class="col s6" name=<%=companies.id%>>
            <input type="hidden" name="id" value=<%=companies.id%> />
            <button type="submit" class='red darken white-text btn-flat'>What's this money going towards?</button>
            </form>
            <form action="user/save-content" method="post" class="col s6" name=<%=companies.length%>>
            <input type="hidden" name="client_name" value=<%=companies.client_name%>>
            <input type="hidden" name="registrant_name" value=<%=companies.registrant_name%>>
            <input type="hidden" name="amount" value=<%=companies.amount%>>
            <button type="submit" class='saveBill blue darken white-text btn-flat'>Save</button>
            </form>
          </div>
          </div>
          <% }) %>
          </div>
          </div>
          <% }else{ %>
          <li>No results</li>
          <% } %>
        </div>
      <script type="text/javascript" src="/js/script.js"></script>
      <script src='https://cdnjs.cloudflare.com/ajax/libs/d3/4.2.7/d3.min.js'></script>
</div>
</body>
</html>
